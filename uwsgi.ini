[uwsgi]
http-socket= :$(PORT) #This will read from Heroku configuration
master=true #Master process will control other slave process
die-on-term=true #This will kill process as it terminates to ave up resources
module=app:app  #This tells module is inside app file and in app variable
memory-report=true